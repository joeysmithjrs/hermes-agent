# Refuse to install on an unsupported Node rather than warning and continuing.
#
# `@polymarket/client` declares engines.node ">=24" in every published version.
# Without this, npm prints an EBADENGINE warning and installs anyway, and the
# mismatch resurfaces later as an obscure runtime failure inside the SDK.
engine-strict=true
