FRESH GLM START (z-ai/glm-5.2, CCR/OR, NOT Pro).
DO NOT RESUME previous session `80098b02...` or fix `proc_5cfbb37a3b9f`. Those are separate.

Current verified state (independent check just performed):
- Worktree: `feat/pm-desk-control-plane-hardening` (pushed to origin)
- Source: only pre-existing calibration provenance commit (`cdfcef2be`, 317 lines: research types/index/cli + tests) exists in branch; remaining plan tasks NOT done.
- Pro auth: EXPIRED (not needed — you are GLM via `claude-glm` launcher, `CLAUDE_CONFIG_DIR=/home/hermes/.claude`, `ANTHROPIC_MODEL=z-ai/glm-5.2`)
- PR #18 exists (`https://github.com/joeysmithjrs/hermes-agent/pull/18`) — opened honestly with `NOT COMPLETED` disclosure; not merged.
- Plan: `/home/hermes/.hermes/plans/2026-08-01_012049-pm-desk-control-plane-hardening.md`

WHAT YOU MUST DO (complete remaining plan tasks):
Task 4: `optional-projects/pm-desk/src/research/registry.ts` — SHIPPED_RESEARCH_TOOLS (cpi_nowcast_bucket_harness).
Task 5: `approval_class: 'auto_agent' | 'joe_infra' | 'joe_live_risk'` in execution-plan schema + render + prompts (plan/dd).
Task 6: Productize `workflows/pm-thesis-reopen-v0.yaml` (copy/polish from host experiment) + prompt libs if needed.
Task 7: Edge-first brief validator (`CLAIM / WHY GAP / MEASURED / KILLS / IF YOU APPROVE`) in render/schema.
Task 8: Gate resume docs + `after-gate` CLI note.
Task 9: Anti-lazy prompt law (DD/DQ/directive bans on single-curl failures and fixture-as-entry).
Task 10: Integration verify: `npm run check`, `hermes workflow validate` morning + reopen, manual `PM_DESK_LIVE_CPI=1` calibrate (expected ~no_trade given current live mid).

COMMIT each completed task separately (`feat: ...`, `fix: ...`, `docs: ...`).
PUSH when done. Update PR #18 description if needed (honestly — don't fabricate).
PAPER ONLY. NO LIVE TRADING. Verify independently after exit.
